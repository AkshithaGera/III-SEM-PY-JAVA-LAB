import re

print("***** Palindrome *****")

text = input("Enter a word/sentence/number : ")

text = text.lower()
text = re.sub(r'[^a-z0-9]', '', text)
rev_text = text[::-1]


if text == rev_text:
    print(f"{text} is a palindrome!")
else:
    print(f"{text} is not a palindrome!!")